apiVersion: v1
kind: Service 
metadata:
  name: lb-svc
  labels:
    env: cloud
  namespace: ns-manifest
spec:
  type: LoadBalancer
  selector:
    env: cloud 
  ports:
  - name: lb-svc-port
    protocol: TCP
    port: 80
    targetPort: 8080